parameters:
  vshn_sli_reporting:
    =_metadata:
      multi_tenant: true
    namespace: ${_instance}
    namespaceMetadata: {}

    images:
      sli_reporting:
        registry: ghcr.io
        repository: vshn/vshn-sli-reporting
        tag: v0.1.2

    auth:
      user: admin
      password: null

    db_file: /data/data.db
    storage_requests: 1Gi

    lieutenant:
      k8s_url: null
      sa_token: null
      namespace: lieutenant

    network_policies:
      target_namespaces: {}


    extra_volumes: {}
